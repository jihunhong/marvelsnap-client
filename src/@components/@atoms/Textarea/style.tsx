import styled from 'styled-components';

export const StyledTextarea = styled.textarea`
  resize: none;
  outline: none;
  border: none;
  background: none;
  overflow: hidden;
  color: var(--white);
  font-size: 1rem;
  padding: 0;
`;
