import { ButtonTag } from '@atoms/Button/style';
import styled from 'styled-components';

export const MoreButtonContainer = styled(ButtonTag)`
  width: 100%;
  height: 32px;
  svg {
    margin: 0;
  }
`;
